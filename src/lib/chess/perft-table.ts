/** Start-position node counts — the generator's receipt, shown in the colophon. */
export const START_PERFT = [
  { depth: 1, nodes: 20 },
  { depth: 2, nodes: 400 },
  { depth: 3, nodes: 8902 },
] as const;
